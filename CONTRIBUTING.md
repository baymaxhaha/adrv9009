# Contributing

## 提交流程

1. 从 `work` 拉分支：`feature/<name>`
2. 完成开发后执行：
   - `make build`
   - `make test`
3. 提交 PR，描述背景、改动、验证方式、风险

## Commit 建议

使用 Conventional Commits，例如：

- `feat: add parser for xxx`
- `fix: handle null pointer in xxx`
- `docs: update onboarding guide`
